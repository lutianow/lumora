#!/bin/bash
# Script para rodar o Vite (frontend) em modo desenvolvimento via Docker
# Uso: ./bin/npm-dev

docker run --rm -it \
  -v "$PWD":/app \
  -w /app \
  -p 5173:5173 \
  node:18 \
  sh -c "npm install && npm run dev -- --host"